!SLIDE execute
# Executable Coffeescript

```coffeescript
add = (a, b) ->
  "#{a}+#{b} is #{a+b}"

result = add 2, 3
```
> CoffeeScript needs to store the results in `result` for the value to appear
in the output.